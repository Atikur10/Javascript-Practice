console.log('Hello World!');

const employee = {
    name: 'Badhon',
    age: 24,
    salary: 5000,
    isMoney: true,
}

const keys = Object.keys(employee);
console.log(keys)
const value = Object.values(employee);
console.log(value)
const entries = Object.entries(employee);
console.log(entries)
// Object.seal, Object.freeze, delete employee.age






















